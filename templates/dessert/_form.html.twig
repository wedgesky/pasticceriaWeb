{{ form_start(form) }}
{#    {{ form_widget(form) }}#}

    <div class="row">
        <div class="col-md-10 ml-md-auto">
            <div class="card bg-light mb-3 mt-5" style="width: 800px">
                <div class="card-body">
                    {{ form_start(form) }}
                    <div class="form_group">
                        <div class="col-md-12 mb-3">
                            {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
                        </div>
                    </div>
                    <div class="form_group">
                        <div class="col-md-12 mb-3">
                            {{ form_row(form.price, {'attr': {'class': 'form-control'}}) }}
                        </div>
                    </div>
                    <div class="form_group">
                        <div class="col-md-12 mb-3">
                            {{ form_row(form.dateSell) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-8 col-md-offset-4" style="margin-top:5px;">
                            <button class="btn btn-primary"><i class="fa fa-btn fa-user"></i>{{ button_label|default('Save') }}</button>
{#                            <button class="btn btn-secondary" href="{{ path('dessert_index') }}"><i class="fa fa-btn fa-user"></i>{{ button_label|default('Lista') }}</button>#}
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{{ form_end(form) }}
